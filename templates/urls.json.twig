urls: {
    player: {
        list: "{{ path('playersList')}}",
        form: "{{ path('playerForm') }}",
        post: "{{ path('playerPost') }}",
        delete: "{{ path('playerDelete') }}"
    },
    court: {
        list: "{{ path('courtsList')}}",
        form: "{{ path('courtForm') }}",
        post: "{{ path('courtPost') }}",
        delete: "{{ path('courtDelete') }}"
    }    
}
