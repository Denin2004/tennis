urls: {
    player: {
        list: "{{ path('playersList')}}",
        form: "{{ path('playerForm') }}",
        post: "{{ path('playerPost') }}",
        delete: "{{ path('playerDelete') }}",
        search: "{{ path('playerSearch') }}"
    },
    court: {
        list: "{{ path('courtsList')}}",
        form: "{{ path('courtForm') }}",
        post: "{{ path('courtPost') }}",
        delete: "{{ path('courtDelete') }}"
    },
    competition: {
        list: "{{ path('competitionsList')}}",
        addForm: "{{ path('competitionAddForm') }}",
        edit: "{{ path('competitionEdit') }}",
        post: "{{ path('competitionPost') }}",
        delete: "{{ path('competitionDelete') }}",
        competitor: {
            list: "{{ path('competitorsList')}}",
            form: "{{ path('competitorForm') }}",
            post: "{{ path('competitorPost') }}",
            delete: "{{ path('competitorDelete') }}"
        }
    }
}
